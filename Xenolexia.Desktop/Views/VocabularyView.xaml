<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Xenolexia.Desktop.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Xenolexia.Desktop.Views.VocabularyView"
             x:DataType="vm:VocabularyViewModel">

    <Grid RowDefinitions="Auto,*" Margin="10">
        <Button Grid.Row="0"
                Content="Export"
                Command="{Binding ExportVocabularyCommand}"
                HorizontalAlignment="Left"
                Margin="0,0,0,10" />

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Vocabulary}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Source Word" Binding="{Binding SourceWord}" Width="*" />
                <DataGridTextColumn Header="Target Word" Binding="{Binding TargetWord}" Width="*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</UserControl>
