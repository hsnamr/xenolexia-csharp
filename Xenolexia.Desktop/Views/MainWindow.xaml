<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Xenolexia.Desktop.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="Xenolexia.Desktop.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="Xenolexia"
        Width="1000" Height="700">

    <Grid RowDefinitions="Auto,*">
        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="Library">
                <ContentControl Content="{Binding LibraryView}" />
            </TabItem>
            <TabItem Header="Vocabulary">
                <ContentControl Content="{Binding VocabularyView}" />
            </TabItem>
            <TabItem Header="Settings">
                <TextBlock Text="Settings - Coming Soon" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center" />
            </TabItem>
            <TabItem Header="About">
                <ContentControl Content="{Binding AboutView}" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
