<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Xenolexia.Linux.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Xenolexia.Linux.Views.LibraryView"
             x:DataType="vm:LibraryViewModel">

    <Grid RowDefinitions="Auto,*" Margin="10">
        <Button Grid.Row="0"
                Content="Refresh"
                Command="{Binding LoadBooksCommand}"
                HorizontalAlignment="Left"
                Margin="0,0,0,10" />

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Books}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*" />
                <DataGridTextColumn Header="Progress" Binding="{Binding Progress, StringFormat='{}{0:F1}%'}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</UserControl>
